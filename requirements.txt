pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
openpyxl>=3.1
matplotlib>=3.6
# TabPFN backend. Runs on the offline stand-in until this is installed + a token
# is set. Install for real foundation-model numbers:
tabpfn>=2.0

# OPTIONAL — canonical time-series features (catch22). If installed, the pipeline
# uses the real 22 hctsa features (columns c22_*); if not, it falls back to a
# labelled numpy subset (columns c22fb_*). Wheels may not exist yet for very new
# Python, so this is optional:  pip install pycatch22
# pycatch22>=0.4

# OPTIONAL comparator for fig1a. Heavy, separate stack, often won't install on
# bleeding-edge Python — if absent, the comparator falls back to a labelled
# classical trend forecaster (ts_trend_baseline). Install only if you want the
# real TimesFM bar:  pip install timesfm
# timesfm>=2.5
